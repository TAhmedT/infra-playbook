- name: Update all packages
  yum:
    name: "*"
    update_cache: yes
    state: latest

- name: Install third-party packages
  yum:
    name: "{{ item }}"
    state: present
  loop:
    - vim
    - bash-completion
    - python3